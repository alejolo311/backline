# Database (PostgreSQL via Supabase or Railway)
DATABASE_URL="postgresql://user:password@host:5432/encore?schema=public"

# NextAuth
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Producer credentials (used only in seed; stored hashed in DB)
SEED_PRODUCER_USERNAME="producer"
SEED_PRODUCER_PASSWORD="changeme123"
